---
requires_ansible: '>=2.18.0'
plugin_routing:
  modules:
    interfaces_assignments:
      deprecation:
        removal_version: 7.0.0
        warning_text: >-
          OPNsense 26.7 migrated interface assignments to MVC/API; use the upstream
          API directly or another collection that supports the new assignments API.
    system_high_availability_settings:
      deprecation:
        removal_version: 5.0.0
        warning_text: Use oxlorg.opnsense.hasync_general and oxlorg.opnsense.hasync_service instead.
